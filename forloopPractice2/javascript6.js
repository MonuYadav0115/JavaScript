// The Code below can reverse an array.


const nums = [1,2,3,4,5]

const lastarray = num.length - 1
const newArray = []
for(let i = lastarray; i >= 0; i--){
    newArray.push(num[i])
} 
console.log(newArray)
